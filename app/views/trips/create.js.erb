<% if @trip.errors.any? %>
  $('#new-trip-errors').html("<%= escape_javascript form_errors(@trip) %>");
<% else %>
  $('#new-trip-errors').html('');
  $('#new_trip')[0].reset();

  $('#table-trips .tbody').prepend('<%= escape_javascript render @trip %>')
<% end %>
