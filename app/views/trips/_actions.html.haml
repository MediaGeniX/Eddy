= link_to "#{fa_icon :edit} Edit".html_safe, edit_user_trip_path(trip.user, trip), class: "btn btn-primary-outline btn-sm", remote: true
- if can? :destroy, trip
  = link_to "#{fa_icon :trash} Delete".html_safe, user_trip_path(trip.user, trip), class: "btn btn-warning-outline btn-sm", data: {confirm: 'Are you sure?'}, remote: true, method: :delete
