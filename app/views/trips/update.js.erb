<% if @trip.errors.any? %>
  $('#edit-trip-errors').html("<%= escape_javascript form_errors(@trip) %>")
<% else %>
  $('#edit-trip-errors').html('')
  $('#edit_<%= dom_id(@trip) %>').replaceWith("<%= j render @trip %>");
<% end %>
